# HalluCheck v4 — 100% FREE install manifest
# ============================================
# Zero paid API keys required.
# Evidence: Wikipedia API (free) + DuckDuckGo (free)
# LLM:      Ollama (free, local)  — install: https://ollama.com
#           then: ollama pull llama3.1:8b
# NLI:      DeBERTa-v3-large (free, auto-downloaded by sentence-transformers)
#
# Install:  pip install -r requirements-free.txt
#           python -m spacy download en_core_web_sm

# Core
openai>=1.30.0            # Ollama OpenAI-compat client (no key needed for Ollama)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Vector store (free, local)
chromadb>=0.5.0

# Hybrid search
rank-bm25>=0.2.2

# NLI + cross-encoder reranker (free, local, GPU-accelerated)
sentence-transformers>=3.0.0
torch>=2.1.0

# Document ingestion
pypdf>=4.0.0
beautifulsoup4>=4.12.0

# Free web search — Wikipedia (built-in urllib, no extra install)
# Free web search — DuckDuckGo (no API key)
ddgs>=9.0.0

# Wikipedia KB auto-seed
wikipedia-api>=0.6.0

# NLP
spacy==3.7.5
thinc==8.2.5

# Cache (optional — in-memory fallback if Redis not running)
redis>=5.0.0

# HMM + math
hmmlearn>=0.3.0
numpy>=1.26.0

# GPU monitoring
pynvml>=11.5.0

# Environment / runtime
python-dotenv>=1.0.0
pyyaml>=6.0.1
rich>=13.7.0
typer>=0.12.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
